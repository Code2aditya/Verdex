# ── Core ML (CPU-only) ───────────────────────────────────
numpy>=1.24
scipy>=1.11
Pillow>=10.0
opencv-python>=4.8

# ── OCR (Module 1) — EasyOCR primary, PaddleOCR optional ─
easyocr>=1.7
# paddleocr / paddlepaddle deliberately OMITTED: adds ~2GB and
# unreliable wheels on slim images. EasyOCR is the working engine.

# ── Tampering Detection (Module 3) ────────────────────────
torch>=2.0
torchvision>=0.15
zxing-cpp>=2.2

# ── Face Verification (Module 4) — InsightFace primary ───
onnxruntime
insightface>=0.7
# mediapipe / deepface OMITTED: InsightFace 3D landmarks drive
# both geometry-based liveness and face embedding; no other
# model is required by the pipeline.

# ── Backend / API ─────────────────────────────────────────
fastapi>=0.104
uvicorn[standard]>=0.24
pydantic>=2.0
pydantic-settings>=2.0
python-multipart>=0.0.6
httpx>=0.25
sqlalchemy>=2.0
PyYAML>=6.0
python-dateutil>=2.8

# ── Blockchain & Security ─────────────────────────────────
web3>=6.0        # only used for LIVE Sepolia anchoring (optional)
pyjwt>=2.8
cryptography>=41.0
